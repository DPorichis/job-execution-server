#pragma once

// itoa and atoi implementations (I wasn't sure if
// the standard implementations are available in linux lab).

// itoa implementation
char* my_itoa (int value);

// atoi implementation
int my_atoi (char* string);